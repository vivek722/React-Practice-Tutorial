export const Practise = () =>{
    const student = []
    return (
        <>
        <h1>{student.length ? "no of student" : student.length}</h1>
        <h1>no of student{}</h1>
        </>
    );
}

// export default Practise;